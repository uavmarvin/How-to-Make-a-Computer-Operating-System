#include "Shell.h"
/*
void Shell::loop(void)
{
	char* line;
	char** args;
	int status;
	
	do
	{
		printf("> ");
		line = readLine();
		args = splitLine(line);
		status = execute(args);
		
		free(line);
		free(args);
	} while(status);
}

char* Shell::readLine(void)
{
	while (true)
	{
		
	}
}

char** Shell::splitLine(char* line)
{
}
*/
